# Tests run as "qa": the ticket mirror is a no-op and no SIP infrastructure
# (SSH tunnel, MySQL pool) is created — @SpringBootTest contexts must never
# reach the real dev VM or write test tickets into the real mirror table.
itg.env=qa
