spring:
  jpa:
    show-sql: true

logging:
  level:
    com.exati.itg:                                DEBUG
    org.hibernate.SQL:                            DEBUG
    org.hibernate.orm.jdbc.bind:                  TRACE
    org.springframework.web:                      INFO
